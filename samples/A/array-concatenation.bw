---
  Array concatenation

  https://rosettacode.org/wiki/Array_concatenation

  === Task ===
  Show how to concatenate two arrays in your language.
  If this is as simple as array1 + array2, so be it.
---

func main() -> {
    let array1 = [1, 2, 3];
    let array2 = [4, 5, 6];

    println array1 && array2; -- [1, 2, 3, 4, 5, 6]
}