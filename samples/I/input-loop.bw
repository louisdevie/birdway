---
  Input loop

  https://rosettacode.org/wiki/Input_loop

  === Task ===
  Read from a text stream either word-by-word or line-by-line until the stream runs out of data.
  The stream will have an unknown amount of data on it.
---

func main() -> {
    let $buffer = "";

    let stream = STDIN;

    -- read line by line
    while not eof(stream) do readln buffer from stream;
};